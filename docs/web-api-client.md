# Web API Client

The repository includes a typed API client template for future frontend integrations.

## Current location

`tooling/web-api-client-template`

## Notes

- centralizes base URL configuration
- keeps auth and capsule calls in separate modules
- uses a small fetch-based abstraction to stay framework-light

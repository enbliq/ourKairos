# Observability Baseline

The repository includes an Express observability template for structured logging and error handling.

## Capabilities

- request IDs
- structured request completion logs
- typed application errors
- normalized 4xx and 5xx error responses

## Current location

`tooling/express-observability-template`

This keeps future API work consistent without requiring the API package to exist on `main`.

# Capsule Routes

The repository includes a controller and router template for capsule CRUD operations.

## Current location

`tooling/capsule-routes-template`

## Notes

- controllers stay thin
- route registration is centralized
- service dependencies are injected rather than imported directly
